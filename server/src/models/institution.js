class Institution {
  constructor(name, profile, authorCount, documentCount, country, city) {
    this.name = name;
    this.profile = profile;
    this.authorCount = authorCount;
    this.documentCount = documentCount;
    this.country = country;
    this.city = city;
  }
}

module.exports = Institution;
